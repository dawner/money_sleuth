<% content_for(:page_title) { 'Institutions' } %>
<% content_for(:breadcrumbs)  do %>
  <li><%= link_to 'Institutions', institutions_path %></li>
  <li class="is-active"><a href="#" aria-current="page"><%= @institution.name%></a></li>
<% end %>

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @institution.name %>
</p>

<p>
  <strong>Slug:</strong>
  <%= @institution.slug %>
</p>

<%= link_to 'Edit', edit_institution_path(@institution) %>
