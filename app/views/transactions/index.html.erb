<% content_for(:page_title) { 'Transactions' } %>

<div class="container">
  <p id="notice"><%= notice %></p>

  <%= render 'transaction_filters', filters: @filters %>
  <%= render 'transactions_table', transactions: @transactions %>
  <br>
  <%= link_to 'Upload new transactions', new_transaction_batch_path %>
</div>
